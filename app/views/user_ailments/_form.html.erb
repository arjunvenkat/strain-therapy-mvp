<%= form_for(@user_ailment) do |f| %>
  <% if @user_ailment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_ailment.errors.count, "error") %> prohibited this user_ailment from being saved:</h2>

      <ul>
      <% @user_ailment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :ailment_id %><br>
    <%= f.number_field :ailment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
